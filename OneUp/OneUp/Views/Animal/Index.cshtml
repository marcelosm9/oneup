@model IEnumerable<OneUp.Models.Animal>

@{
    ViewBag.Title = "Animal";
}

<h2>Animal</h2>

<p>
    @Html.ActionLink("Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacao)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.AnimalID }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.AnimalID }) |
            @Html.ActionLink("Apagar", "Delete", new { id=item.AnimalID })
        </td>
    </tr>
}

</table>
